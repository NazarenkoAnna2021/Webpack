export const dom = {
    mainArea: document.querySelector('.main-area') as HTMLDivElement,
    templateCard: document.querySelector('.template-card').innerHTML as string,
    userInfo: document.querySelector('.user-info') as HTMLDivElement,
    userText: document.querySelector('userText') as HTMLParagraphElement,
    templateInfo: document.querySelector('.template-info').innerHTML as string
}